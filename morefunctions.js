// let someStuff = 4;

//let theAchieved = ()

// this is for react ///////

/* const ItemListTitle = (count) => (
    <h3>Item list{ count ? (<span> - {count} items</span>) : '(Empty)'}<h3>
  ); 


// Code using if...else
const conditionalX = (condition, x) => {
    if (condition) return x;
    else return x + 5;
  }
  // const conditionalX = (condition, x) => condition ? x : x + 5;



The prompt() method displays a dialog box that prompts the user for input.

The prompt() method returns the input value if the user clicks "OK", otherwise it returns null.
Note
// 

A prompt box is used if you want the user to input a value.

When a prompt box pops up, the user will have to click either "OK" or "Cancel" to proceed.

Do not overuse this method. It prevents the user from accessing other parts of the page until the box is closed.



const hasDriversLicense = prompt("Do you have a driver's license? yes/no");

const license = hasDriversLicense === "Yes" ? true : false;

const canDrive = (license) => {
    return `gatermark ${license ? "can" : "can't"} drive`;
};

console.log(canDrive(license));


const theSupper = prompt("Do you want some supper? yes/nope");

const theDecision = theSupper === "yes" ? true : false;

const theFood = (theDecision) => {
    return `So ${theDecision ? "food" : "bleegh"} eat`;
};

console.log(theFood(theDecision));
*/